const puppeteer = require('puppeteer');

let scrape = async () => {
  // Здесь выполняются операции скрапинга...
  return 'test';
  // Возврат значения
};

scrape().then((value) => {
    console.log(value); // Получилось!
});
